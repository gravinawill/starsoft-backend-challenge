{
  "$id": "http://example.com/users-employee-created.schema.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "description": "Schema for the UsersEmployeeCreated event payload for Kafka Schema Registry.",
  "properties": {
    "createdAt": {
      "description": "The ISO 8601 timestamp when the user was created.",
      "format": "date-time",
      "type": "string"
    },
    "email": {
      "description": "The email of the user.",
      "type": "string"
    },
    "name": {
      "description": "The name of the user.",
      "type": "string"
    },
    "updatedAt": {
      "description": "The ISO 8601 timestamp when the user was last updated.",
      "format": "date-time",
      "type": "string"
    },
    "userID": {
      "description": "The unique identifier for the user (UUID).",
      "format": "uuid",
      "type": "string"
    }
  },
  "required": ["userID", "name", "email", "createdAt", "updatedAt"],
  "title": "UsersEmployeeCreated",
  "type": "object"
}
