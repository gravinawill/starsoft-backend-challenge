-- Database initialization script
-- Create users database
CREATE DATABASE users;

-- Create products_inventory database
CREATE DATABASE products_inventory;

-- Optional: Grant permissions to the default postgres user on these databases
-- (This is usually not needed as the postgres user is the owner by default)
-- GRANT ALL PRIVILEGES ON DATABASE users TO postgres;
-- GRANT ALL PRIVILEGES ON DATABASE products_inventory TO postgres;
